import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    message: "Auth endpoint placeholder for Phase 3",
  });
}

export async function POST() {
  return NextResponse.json({
    message: "Auth endpoint placeholder for Phase 3",
  });
}
